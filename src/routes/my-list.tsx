import {createFileRoute} from "@tanstack/react-router";import {catalog} from "@/data/catalog";import {useCinemora} from "@/hooks/use-cinemora";import {Page} from "@/components/layout/Page";import {MediaCard} from "@/components/media/MediaCard";import {EmptyState} from "@/components/feedback/EmptyState";import {Tabs,TabsContent,TabsList,TabsTrigger} from "@/components/ui/tabs";export const Route=createFileRoute('/my-list')({head:()=>({meta:[{title:'My List — CINEMORA'},{name:'description',content:'Your saved CINEMORA stories.'},{property:'og:title',content:'My List — CINEMORA'},{property:'og:description',content:'Your saved CINEMORA stories.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=>{const {list}=useCinemora();const items=catalog.filter(x=>list.includes(x.id));return <Page eyebrow="Your library" title="My List"><Tabs defaultValue="movies"><TabsList><TabsTrigger value="movies">Movies</TabsTrigger><TabsTrigger value="tv">TV Shows</TabsTrigger></TabsList>{(['movies','tv'] as const).map(t=><TabsContent key={t} value={t}>{items.filter(x=>x.type===(t==='movies'?'movie':'tv')).length?<div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5">{items.filter(x=>x.type===(t==='movies'?'movie':'tv')).map(x=><MediaCard key={x.id} media={x}/>)}</div>:<EmptyState title="Your watchlist is waiting." description="Save stories and they’ll appear here."/>}</TabsContent>)}</Tabs></Page>}})
