import {createFileRoute,Link} from "@tanstack/react-router";import {genres,catalog} from "@/data/catalog";import {Page} from "@/components/layout/Page";export const Route=createFileRoute('/genres')({head:()=>({meta:[{title:'Genres — CINEMORA'},{name:'description',content:'Explore stories by genre.'},{property:'og:title',content:'Genres — CINEMORA'},{property:'og:description',content:'Explore stories by genre.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <Page eyebrow="Explore" title="Find your mood"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{genres.map((g,i)=>{const m=catalog[i%catalog.length];return m?<Link key={g} to="/genre/$slug" params={{slug:g.toLowerCase().replace(' ','-')}} className="group relative aspect-[16/8] overflow-hidden rounded-md"><img src={m.backdrop} alt="" loading="lazy" className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-background to-transparent"/><div className="absolute inset-x-0 bottom-0 p-5"><h2 className="font-display text-2xl font-semibold">{g}</h2><p className="text-sm text-muted-foreground">{24+i*7} titles</p></div></Link>:null})}</div></Page>})
