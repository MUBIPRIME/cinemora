import {createFileRoute} from "@tanstack/react-router";import {catalog} from "@/data/catalog";import {Page} from "@/components/layout/Page";import {MediaCard} from "@/components/media/MediaCard";export const Route=createFileRoute('/movies')({head:()=>({meta:[{title:'Movies — CINEMORA'},{name:'description',content:'Discover premium films on CINEMORA.'},{property:'og:title',content:'Movies — CINEMORA'},{property:'og:description',content:'Discover premium films on CINEMORA.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <Page eyebrow="Explore" title="Movies" description="Unforgettable stories, curated for every mood."><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5">{catalog.filter(x=>x.type==='movie').map(x=><MediaCard key={x.id} media={x}/>)}</div></Page>})
