import {createFileRoute} from "@tanstack/react-router";import {Check} from "lucide-react";import {Page} from "@/components/layout/Page";import {Button} from "@/components/ui/button";import {toast} from "sonner";const plans=[['FREE','$0',['HD streaming','1 device','With advertisements']],['CINEMORA PLUS','$9.99',['Full HD streaming','2 devices','Offline downloads','No advertisements']],['CINEMORA ULTRA','$14.99',['4K streaming','4 devices','Offline downloads','No advertisements','Multiple profiles','Premium content']]] as const;export const Route=createFileRoute('/plans')({head:()=>({meta:[{title:'Plans — CINEMORA'},{name:'description',content:'Compare CINEMORA demo membership plans.'},{property:'og:title',content:'Plans — CINEMORA'},{property:'og:description',content:'Compare CINEMORA demo membership plans.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <Page eyebrow="Membership" title="Choose how you watch" description="Demo pricing only. No payment will be collected."><div className="grid gap-5 lg:grid-cols-3">{plans.map((p,i)=><article key={p[0]} className={`rounded-md border p-7 ${i===1?'border-primary bg-card':'border-border'}`}><p className="text-xs font-semibold tracking-[.2em] text-primary">{p[0]}</p><p className="mt-4 font-display text-4xl font-bold">{p[1]}<span className="text-sm font-normal text-muted-foreground">/month</span></p><ul className="my-8 space-y-3">{p[2].map(x=><li className="flex gap-2 text-sm" key={x}><Check className="size-4 text-primary"/>{x}</li>)}</ul><Button className="w-full" variant={i===1?'default':'outline'} onClick={()=>toast.success('Demo plan selected')}>Choose plan</Button></article>)}</div></Page>})
