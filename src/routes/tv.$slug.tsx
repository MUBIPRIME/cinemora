import {createFileRoute,notFound} from "@tanstack/react-router";import {catalog} from "@/data/catalog";import {DetailsPage} from "@/components/media/DetailsPage";export const Route=createFileRoute('/tv/$slug')({loader:({params})=>{const m=catalog.find(x=>x.slug===params.slug&&x.type==='tv');if(!m)throw notFound();return m},head:({loaderData})=>({meta:[{title:`${loaderData?.title??'Series'} — CINEMORA`},{name:'description',content:loaderData?.description??'Series details.'},{property:'og:title',content:`${loaderData?.title??'Series'} — CINEMORA`},{property:'og:description',content:loaderData?.description??'Series details.'},{property:'og:type',content:'video.tv_show'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <DetailsPage media={Route.useLoaderData()}/>})
