import {createFileRoute} from "@tanstack/react-router";import {catalog} from "@/data/catalog";import {Page} from "@/components/layout/Page";import {MediaCard} from "@/components/media/MediaCard";export const Route=createFileRoute('/genre/$slug')({head:({params})=>({meta:[{title:`${params.slug} — CINEMORA`},{name:'description',content:`Explore ${params.slug} stories.`},{property:'og:title',content:`${params.slug} — CINEMORA`},{property:'og:description',content:`Explore ${params.slug} stories.`},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=>{const {slug}=Route.useParams();const genre=slug.replace('-',' ');const items=catalog.filter(x=>x.genres.some(g=>g.toLowerCase()===genre));return <Page eyebrow="Genre" title={genre.replace(/\b\w/g,c=>c.toUpperCase())}><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5">{(items.length?items:catalog.slice(0,10)).map(x=><MediaCard key={x.id} media={x}/>)}</div></Page>}})
