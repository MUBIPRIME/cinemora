import { ChevronRight } from "lucide-react";import { MediaCard } from "./MediaCard";import type { Media } from "@/types/media";
export function MediaShelf({title,items,subtitle}:{title:string;items:Media[];subtitle?:string}){return <section className="py-5"><div className="mb-4 flex items-end justify-between"><div><h2 className="font-display text-xl font-semibold text-foreground sm:text-2xl">{title}</h2>{subtitle&&<p className="mt-1 text-sm text-muted-foreground">{subtitle}</p>}</div><ChevronRight className="size-5 text-muted-foreground"/></div><div className="scrollbar-hide -mx-4 flex gap-3 overflow-x-auto px-4 pb-5 sm:-mx-8 sm:px-8 lg:-mx-12 lg:px-12">{items.map(m=><MediaCard key={m.id} media={m}/>)}</div></section>}
