import { ExternalLink } from "lucide-react";import type { Provider } from "@/types/media";
export function ProviderGrid({providers}:{providers:Provider[]}){return <div><div className="grid gap-3 sm:grid-cols-3">{providers.map(p=><a key={p.name} href={p.url} target="_blank" rel="noreferrer" className="flex items-center justify-between rounded-md border border-border bg-card p-4 transition hover:border-primary"><div><p className="font-semibold">{p.name}</p><p className="mt-1 text-xs capitalize text-muted-foreground">{p.type}</p></div><ExternalLink className="size-4"/></a>)}</div><p className="mt-4 text-sm text-muted-foreground">Streaming availability varies by region.</p></div>}
