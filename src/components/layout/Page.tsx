import type {ReactNode} from "react";export function Page({eyebrow,title,description,children}:{eyebrow?:string;title:string;description?:string;children:ReactNode}){return <div className="mx-auto min-h-[80vh] max-w-7xl px-4 pb-16 pt-28 sm:px-8 lg:px-12">{eyebrow&&<p className="text-xs font-semibold uppercase tracking-[.2em] text-primary">{eyebrow}</p>}<h1 className="mt-2 font-display text-4xl font-bold sm:text-5xl">{title}</h1>{description&&<p className="mt-3 max-w-2xl text-muted-foreground">{description}</p>}<div className="mt-10">{children}</div></div>}
